<!DOCTYPE html>
<html>
<head>
  <title>What Would Bayliss Say?</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
  	<div class="col-xs-3">
  		<%= link_to root_path, :class => 'logo' do %>
				<div class="circle">
				  <br />
				  What would
				  <br />
				  Bayliss
				  <br />
				  Say?
				</div>
			<% end %>
  	</div>
  	<div class="col-sm-4 hidden-xs slogan">
  		What Would Bayliss Say?
  	</div>
  	<div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-5 links">
	    <!-- Button trigger modal -->
			<%= link_to 'Contribute', '#', :data => {:toggle => 'modal', :target => '#myModal'} %>
			&nbsp;&nbsp;
  		<%= link_to 'About', about_path %>
  	</div>
  </header>

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Contribute Your Baylissism</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
	        <div class="modal-body">
	          	<%= f.input :lyric, :label => "What is the Bayliss lyric you want to add?", :placeholder => "The only certain thing is changes." %>
	          	<br />
	          	<%= f.input :song, :label => "What song is it from?", :placeholder => 'Final Word' %>
	          	<br />
	        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
          <%= f.submit 'Create', :class => 'btn btn-danger' %>
         </div>
	      <% end %>
      </div>
    </div>
  </div>


  <hr />
  <br class="clear" />

  <div class="container">
  	<% if flash[:error].present? %>
  		<div class="col-xs-offset-1 alert alert-danger">
			<%= flash[:error].html_safe %>
  		</div>
		<% end %>
		<%= yield %>
	</div>

	<div class="footer">
		<div class="container">
			<div class="col-xs-12 col-sm-6">
				<%= link_to "This isn't enough, I need more Bayliss!", root_path, :class => 'next-quote' %>
			</div>
			<div class="col-xs-12 col-sm-6 footer-blurb">
				Spawned in the People's Republic of Portland
			</div>
		</div>
	</div>

</body>
</html>